apiVersion: v1
kind: Service
metadata:
  name: cpu-intensive-service
  labels:
    app: cpu-intensive-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: cpu-intensive-app
